{
  "name": "electron-excel",
  "version": "1.0.0",
  "description": "It's a test for a executable program on windows to load excel files",
  "main": "main.js",
  "scripts": {
    "start": "nodemon --watch * --exec \"electron .\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JacobKim",
  "license": "ISC",
  "dependencies": {
    "electron": "^11.1.1",
    "eslint": "^7.16.0",
    "nodemon": "^2.0.6",
    "read-excel-file": "^5.0.0"
  }
}
